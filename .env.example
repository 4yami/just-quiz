# Google Drive OAuth — Client ID only (public by design, no secret needed for browser-only OAuth)
# Create one at https://console.cloud.google.com/apis/credentials
# Add your GitHub Pages URL (e.g. https://your-username.github.io) as an Authorized JavaScript origin
NUXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=your-client-id.apps.googleusercontent.com